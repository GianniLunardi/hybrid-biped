SET(PACKAGE_PYTHON_FILES
  Params.py
  HybridLIPM.py
  LinearMPC.py
  LIPM_to_WBD.py
  TSID.py
  inverse_geometry.py
)

STRING(REGEX REPLACE "-" "_" PY_NAME ${PROJECT_NAME})
PYTHON_INSTALL_ON_SITE(${PY_NAME} __init__.py)

FOREACH(python ${PACKAGE_PYTHON_FILES})
  PYTHON_INSTALL_ON_SITE(${PY_NAME} ${python})
ENDFOREACH(python ${PACKAGE_PYTHON_FILES})
